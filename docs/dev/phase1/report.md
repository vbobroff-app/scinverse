# Фаза 1. Отчёт о выполнении

Актуальный статус работ по Фазе 1. Обновляется по мере выполнения задач из [plan.md](plan.md).

**Текущий статус:** `DONE` — `V003` накатан на локальную TimescaleDB, схема верифицирована.
**Обновлено:** 2026-07-07.

## Статус задач

| #   | Задача                                                   | Статус | Комментарий |
| --- | -------------------------------------------------------- | ------ | ----------- |
| 1.1 | Написать `V003__derivative_and_risk.sql`                 | DONE   | DDL — см. [apply.md](apply.md) |
| 1.2 | Накатить мигратор                                        | DONE   | применён ровно один новый скрипт (`V003`) |
| 1.3 | Верифицировать схему (таблицы, FK, индекс, журнал)       | DONE   | все объекты на месте; повтор идемпотентен |
| 1.4 | Обновить статус Решения 2 в `db-design.md` → `DONE`      | DONE   | `PLANNED (V003)` → `DONE (V003)` |

## Критерии приёмки — чек-лист

- [x] Прогон мигратора: код `0`, применён ровно один новый скрипт (`V003`).
- [x] `derivative`: PK `instrument_id` (FK → `instrument`), self-FK `underlying_id`, индекс `ix_derivative_chain`.
- [x] `instrument_risk`: составной PK `(instrument_id, valid_from)`, FK → `instrument`.
- [x] Повторный прогон идемпотентен (0 новых скриптов).
- [x] `dotnet build Ohs.sln` — без ошибок.
- [x] Статус Решения 2 в `db-design.md` → `DONE`.

## Результат верификации

- Таблицы `public`: добавлены `derivative`, `instrument_risk` (всего 7 таблиц вместе с журналом).
- `derivative`: `derivative_pkey` (PK), FK `derivative_instrument_id_fkey` и
  `derivative_underlying_id_fkey` (self-FK на `instrument`), CHECK `ck_derivative_option_type`,
  индекс `ix_derivative_chain`.
- `instrument_risk`: составной PK `pk_instrument_risk (instrument_id, valid_from)`, FK
  `instrument_risk_instrument_id_fkey`.
- `schemaversions`: три записи (`V001`, `V002`, `V003`).

## Лог выполнения

| Дата | Действие | Результат |
| ---- | -------- | --------- |
| 2026-07-07 | Создан план Фазы 1 (`docs/dev/phase1/**`) | Документы созданы |
| 2026-07-07 | 1.1 написан `V003__derivative_and_risk.sql` | Миграция добавлена |
| 2026-07-07 | 1.2 `dotnet run` мигратора | Применён `V003` (1 новый скрипт) |
| 2026-07-07 | 1.3 верификация + повторный прогон | Схема корректна; повтор = 0 скриптов |
| 2026-07-07 | `dotnet build Ohs.sln` | 0 ошибок, 0 предупреждений |
| 2026-07-07 | 1.4 `db-design.md` Решение 2 → `DONE` | Документ обновлён |

## Следующий шаг

Фаза 1 завершена. Далее — Фаза 3 (проверки в необходимом объёме) либо Фаза 2 (OrderLog/мультиисточник)
при появлении второго источника данных.
