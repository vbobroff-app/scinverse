# Phase 7j — TODO (UI schedule)

Будущие доработки UI расписания соединения. Не блокируют текущий UX.

## График дат · пагинация по месяцам

**Контекст.** Сейчас static/confirm-календарь рисует непрерывный диапазон дат одной лентой (подписи уплотняются: при >14 колонках — только число дня). Для диапазонов **длиннее ~1 месяца** колонки становятся слишком узкими.

**Сделать (позже):**

- Обрабатывать диапазоны **> 1 мес** через **пагинацию по месяцам** на графиках (edit static + confirm «Новое» / «Что изменится»).
- По краям рабочей области графика — **полупрозрачный chrome** (левый / правый), клик (или hold) **пролистывает данные на соседний месяц**.
- В видимой области — один «страничный» месяц (или окно ≤ ~31 день), без сжатия всей multi-month ленты в одну полосу.

**Пока:** оставляем текущее поведение (полная лента + уплотнение подписей).
